# Ad-hoc run output stays local. The named reference runs the README cites are
# tracked, because a baseline nobody else has is not a baseline.
*.json
!baseline.json
!rule7-tightened.json
!prompt-cleanup.json
